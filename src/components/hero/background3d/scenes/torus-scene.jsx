// Deprecated: empty placeholder to keep path stable during migration.
if (typeof process !== 'undefined' && process.env && process.env.NODE_ENV !== 'production') {
  console.warn('[deprecated] TorusScene.jsx is deprecated. Use TorusGardenScene.tsx instead.');
}
export default function TorusScene() {
  return null;
}
