import { tokens } from '../design/tokens';

// Create shadow array using tokens
const createShadows = () => {
  // Default shadows
  return [
    'none',
    tokens.shadows.xs, 
    tokens.shadows.sm,
    tokens.shadows.sm,
    tokens.shadows.md,
    tokens.shadows.md,
    tokens.shadows.md,
    tokens.shadows.lg,
    tokens.shadows.lg,
    tokens.shadows.lg,
    tokens.shadows.xl,
    tokens.shadows.xl,
    tokens.shadows.xl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
    tokens.shadows.xxl,
  ];
};

const shadows = createShadows();

export default shadows;
